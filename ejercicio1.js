const base = parseInt(prompt("Dame la base del rectángulo:"));
const height = parseInt(prompt("Dame la altura del rectángulo:"));
const area = base * height;
console.log(`El área del rectángulo es ${area}`);